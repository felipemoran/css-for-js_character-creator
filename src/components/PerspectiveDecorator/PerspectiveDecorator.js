import React from 'react';

import styles from './PerspectiveDecorator.module.css';

const PerspectiveDecorator = () => {
  return (
    <div className={styles.perspective_decorator}>
    </div>
  );
};

export default PerspectiveDecorator;
